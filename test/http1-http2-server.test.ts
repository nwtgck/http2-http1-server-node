// TODO: You should change this content.

import {hoge}  from '../lib/http1-http2-server';
import * as assert from 'power-assert';

describe('hoge', () => {
  it('should return the same value as string length', () => {
    assert.equal(hoge("hello"), 5);
  });
});
